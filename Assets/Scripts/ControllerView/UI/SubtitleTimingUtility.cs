// SubtitleTimingUtility now lives in SubtitlePanelBase.cs so it stays inside the
// existing Unity-generated Assembly-CSharp project file without requiring a manual
// project refresh. This file intentionally stays empty to avoid duplicate symbols
// when Unity regenerates the solution later.
